/target
.env
/grpc-server/target
/grpc-server/.env
/book
# mermaid assets are installed by `mdbook-mermaid install` (CI does this every
# docs build) — never commit them, wherever they land
mermaid.min.js
mermaid-init.js
.DS_Store
.cargo/config.toml
.claude/
# Local nginx source downloaded for the nginx-sys bindings (any version)
/nginx-*/
nginx*.tar.gz
