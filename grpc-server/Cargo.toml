[package]
name = "grpc-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.10"
tonic-reflection = "0.10"
prost = "0.12"
tokio = { version = "1.0", features = ["full"] }
log = "0.4"
env_logger = "0.10"

[build-dependencies]
tonic-build = "0.10"
